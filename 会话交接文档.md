# r1-panel 开发交接

更新时间：2026-08-09（Asia/Shanghai）

## 2026-08-09 修复：弹窗丢帧导致画面冻结

- 现象：服务页滚动到底部点击重启，确认弹窗不出现且整屏冻结。
- 根因：`present()` 在上一帧 page flip 未完成时直接丢帧（display.rs）；弹窗打开后主循环所有重绘路径被 `overlay == Overlay::None` 门控（main.rs），不会再调用 `present()` → 弹窗帧永不上屏。
- 修复：点击处理处等待 pending flip 完成（≤60ms）再 present；弹窗打开时允许 full redraw 继续跑（`view::draw` 会重绘弹窗）作为丢帧兜底，dynamic redraw 仍跳过避免遮罩闪烁。
- 验证构建 `r1-panel-rust-39f90cc4`（SHA256 39f90cc4…），NAS 上以 `RUST_LOG=debug` 运行中（rust-39f90cc4.log）。

## 2026-08-09 调整：重启/关机改为完全优雅操作

- 用户要求最大化安全。`execute_power` 不再传 `--force`：`sync` 落盘后执行纯 `systemctl reboot/poweroff`，走 systemd 完整关停序列（服务 SIGTERM 收尾 → 干净卸载文件系统）。`sync` 移入后台线程避免阻塞主循环。
- 验证构建 `r1-panel-rust-9a27552b`（SHA256 9a27552b…），NAS 运行中（rust-9a27552b.log）。
- 注意：若某服务卡死，systemd 会在 StopTimeout（默认 90s）后 SIGKILL 它并继续关停，不会无限挂起。

## 2026-08-09 修复：重启后首次深刷新导致灰屏

- 现象：服务器重启后约 1 分钟屏幕变灰屏；`dmesg` 出现 i915 警告 `Missing case (intel_dsi->video_mode == 3)`（gen11_dsi_pre_enable），触发时刻恰为面板启动后 +60s 的首次深刷新（pipe 关/开循环）。服务重启（全新 enable）可恢复屏幕；深刷新在系统稳定后执行无害。
- 修复：`display.rs` 首次深刷新从启动后 60s 推迟到 600s，避开系统刚启动、DSI 链路未稳定的脆弱窗口；之后仍维持 5 分钟周期。
- 验证构建 `r1-panel-rust-28f9262f`（SHA256 28f9262f…），已替换 NAS 的 `/opt/r1-panel/r1-panel`（旧版备份为 `r1-panel.v0.2.0.bak`），systemd 服务运行中（PID 44735）。

## 2026-08-11 改进：eMMC 磁盘显示与文档

- eMMC 无通电时长计数器（EXT_CSD 无此项、smartctl 不支持 MMC），不显示使用时长；改为显示：已耗寿命区间（`life_time` 0x0..0xB 编码）、健康徽章（`pre_eol_info` 0x01/0x02/0x03 → 正常/注意/告警）、出厂日期（`date`）。修复原 `life_time` 换算错误（0x01 曾显示 0%，实为 10~20%）。
- HDD 补解析 SMART 属性 `Power_On_Hours`（此前 ATA 属性格式未匹配，16402h 未显示）。
- 健康徽章区分三态：正常（绿）/注意（黄）/告警（红）。
- 说明文档已加入 `rust-lvgl-panel/README.md`「磁盘健康指标说明」。
- 验证构建 `r1-panel-rust-69d2f93c`（SHA256 69d2f93c…），已用真实 NAS 数据截图核对（eMMC 行：Y0S256 / eMMC 233G 正常 系统 / 出厂2022-11 · 已耗10~20%）。

## 2026-08-11 修复（用户反馈）：字形缺失、HDD 温度/坏道、eMMC 精简

- 现象：新加的"出厂/已耗"等文字在面板上显示为"??"。
- 根因：`render.rs` 字符集（`chars.extend(...)`）缺少 `出厂已意注坏道` 等字形，缺失字符一律回退渲染为 `?`。
- 修复：字符集补充 `已注意坏道`；按用户要求 eMMC 不再显示使用时长与出厂日期（移除 `manufactured` 字段及显示）。
- HDD 补充解析：`Temperature_Celsius`（194 属性）温度、`Reallocated_Sector_Ct`（5 属性）坏道数；ATA 属性 raw 值取括号前最后一个字段。
- 验证构建 `r1-panel-rust-fb4d9fbb`（SHA256 fb4d9fbb…）已安装运行（PID 2310759）；截图核对：eMMC 行 `已耗10~20%` 无 ?，HDD 行 `16402h · 44℃ · 坏道0`。

## 2026-08-11 调整：页面拆分（4 页）

- 虚拟机、电源操作从服务页拆出，独立成页：概况 / 服务（核心服务+Docker）/ 虚拟机 / 电源操作，横向翻页循环 4 页。
- `view.rs`：`Page` 增加 Vms/Power；`service_layout` 简化为服务页双块；`hit_test` 仅在 Power 页生效；页点 2→4；电源块固定在 y=92（不再需滚动）。
- `main.rs`：`scroll` 数组 2→4；截图模式支持 `R1_PANEL_PAGE=vms|power`。
- 验证构建 `r1-panel-rust-e3e51f39`（SHA256 e3e51f39…）已安装运行（PID 2360597）；4 页截图与弹窗截图核对通过。

## 分支与主线

- `master`：Rust 原生 DRM/KMS 主线，后续开发应在此分支进行。
- `cog`：旧 `master` 的 Go + Cog/WebKit 实现备份，基线为 `450016a`。
- `rust-lvgl`：主线迁移前的开发分支；迁移完成后以 `master` 为准。

旧 Go 后端、Web 前端和 LVGL FFI 已从新主线清理，需要参考时从 `cog` 分支读取，不要复制回 `master`。

## 当前架构

- `display.rs`：DRM/KMS 双 dumb buffer、page flip 和显示链路维护。
- `input.rs`：evdev 多点触摸、纵向滚动、横向翻页和轴向迟滞。
- `interaction.rs`：惯性滚动与 280 ms 切页/回弹动画。
- `metrics.rs`：快速/慢速指标模型及采集。
- `metrics_worker.rs`：容量 1 的有界通道，500 ms 快采样、5 s 慢采样。
- `render.rs`：纯 Rust 软件栅格器、字体缓存和 BMP 输出。
- `view.rs`：`376×960` 两页布局、局部动态刷新和确认弹窗。

## 已确认的界面要求

- 网速区域：箭头左侧固定；单位右侧固定；数值右对齐；数字与单位字号一致；单位为 `B/s`、`KB/s`、`MB/s`、`GB/s`；内容整体垂直居中。
- 快速指标每 500 ms 更新数据和动态区域，不让整个显示区域消失后重现。
- CPU、内存主数值比初版缩小一号。
- Docker 行只显示服务名和右侧固定状态，两者在原行高内垂直居中；不显示 `Up 2 weeks`，不预留下方空白。
- IP、存储、状态、系统、进度条文字、服务状态及弹窗按钮文字均需在各自框内垂直居中。

## 构建验证

在 Linux/WSL 执行：

```bash
cd rust-lvgl-panel
cargo fmt --all --check
cargo check --locked --target x86_64-unknown-linux-gnu
cargo test --locked --target x86_64-unknown-linux-musl
cargo build --locked --release --target x86_64-unknown-linux-musl
```

截图模式不会打开 DRM：

```bash
R1_PANEL_PAGE=overview ./target/x86_64-unknown-linux-musl/release/r1-panel --screenshot overview.bmp
R1_PANEL_PAGE=services R1_PANEL_SCROLL=900 ./target/x86_64-unknown-linux-musl/release/r1-panel --screenshot services.bmp
R1_PANEL_MODAL=reboot ./target/x86_64-unknown-linux-musl/release/r1-panel --screenshot modal.bmp
```

## 资源与稳定性约束

- 运行时 UI 图标位于 `assets/emoji/` 并编译进二进制；`assets/emoji.ttf` 只用于图标再生成工具。
- 快慢采样通道有界，不允许改成会无限积压的队列。
- 慢速外部命令不得放回触摸/渲染主循环。
- DRM 缓冲区、映射和后台线程退出路径必须继续成对释放。
- 根目录实验截图、RAW 和测试二进制是本地观察资料，已通过 `.gitignore` 排除，不批量删除或提交。

## NAS 测试边界

- 只上传、启动和停止本项目测试版本。
- 每个构建使用 SHA256 前 8 位的新文件名，不覆盖或删除旧版本、日志和截图。
- 停止进程前重新运行 `ps -ef | grep '[r]1-panel'` 并核对完整命令行，优先发送 `SIGTERM`。
- 未经用户再次明确授权，不安装/更新软件包，不修改系统服务、开机项或其他系统配置。
- 设备不在用户身边时，部署后使用截图模式或帧缓冲捕获提供页面截图。
